<!DOCTYPE html>
<html>

<head>
    <title>db.ejs</title>
    <% include ../partials/html_header.html %>
</head>

<body>
    <% include ../partials/html_body_header.html %>
        <div class="container">
            <h2>Database Results</h2>
            <ul>
                <% results.forEach(function(r) { %>
                    <li>
                        <%= r.id %> -
                            <%= r.name %>
                    </li>
                    <% }); %>
            </ul>
        </div>
        <% include ../partials/html_body_ooter.html %>
</body>

</html>